html
  head
    title= title
  body
    h1= note.title
    p= note.content
    a(href='/notes/' + note._id + '/edit') Edit
    form(action='/notes/' + note._id + '?_method=DELETE', method='POST')
      input(type='submit', value='DELETE')
