html
  head
    title= title
  body
  h1= message
  ul
    each file in files
      li
        a(href='/upload/files/' + file._id) file.title
        a(href='/upload/files/' + file_id + '/edit') Edit
        form(action='/upload/files/' + file._id + '?_method=DELETE', method='POST')
          input(type='submit', value='DELETE')
