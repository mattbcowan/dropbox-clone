html
  head
    title= title
  body
    h1= file.title
    p= file.type
    a(href='/upload/files/' + file._id + '/edit') Edit
    form(action='/upload/files/' + file._id + '?_method=DELETE', method='POST')
      input(type='submit', value='DELETE')
